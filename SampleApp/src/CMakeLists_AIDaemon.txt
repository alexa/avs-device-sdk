
set(AIDAEMON_SOURCES)
if (OBIGO_AIDAEMON)
    list(APPEND AIDAEMON_SOURCES
        AIDaemon-dbus-generated.c
        base64.cpp)
endif()

set(AIDAEMON_INCLUDES)
if (OBIGO_AIDAEMON)
    list(APPEND AIDAEMON_INCLUDES
        "/usr/include/glib-2.0"
        "/usr/lib/x86_64-linux-gnu/glib-2.0/include"
        "/usr/include/gio-unix-2.0"
        "${SampleApp_SOURCE_DIR}/include/AIDaemon")
endif()

set(AIDAEMON_LIBRARIES)
if (OBIGO_AIDAEMON)
    list(APPEND AIDAEMON_LIBRARIES
        glib-2.0
        gio-2.0
        gobject-2.0)
endif()
